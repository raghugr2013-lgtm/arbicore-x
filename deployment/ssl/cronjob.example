# ArbiCore X — certbot renewal cron (root crontab)
# Runs daily at 03:17 UTC. Renews certs only within 30 days of expiry.
# Also reloads nginx after any successful renewal.
17 3 * * * /opt/arbicore-x/deployment/ssl/renew.sh >>/var/log/arbicore-x/certbot-renew.log 2>&1
