import { SectionPlaceholder } from "@/v2/components/SectionPlaceholder";

export default function SettingsPage() {
  return <SectionPlaceholder sectionKey="settings" slice={5} journeys={["J8 changing configuration"]} />;
}
