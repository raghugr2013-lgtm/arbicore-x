"""ArbiCore X — execution subsystem package (Wave 6+).

Groups the execution-side substrates that were previously scattered
across `services/execution/` in the canonical repo.  Preview-pod
mirror; the eventual merge lands under the same import path.
"""
